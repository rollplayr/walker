{
  "displayName": "(TESTING) User bibliographic activity (instances, holdings, items)",
  "description": "(TESTING) Counts of instances, holdings, and items created and modified by users, filterable by username and date range.",
  "instructions": "Enter a username (optional) and choose a start and end date for the activity period.",
  "parameters": [
    {
      "name": "username_filter",
      "displayName": "Username",
      "type": "text",
      "required": false
    },
    {
      "name": "start_date",
      "displayName": "Earliest activity date",
      "type": "date",
      "required": false
    },
    {
      "name": "end_date",
      "displayName": "Latest activity date",
      "type": "date",
      "required": false
    }
  ]
}
